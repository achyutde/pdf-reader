PDF Reader
==========

A single-page, client-side web application that enables text-to-speech reading
of PDF documents with advanced controls and bookmarking capabilities.

Features
--------
- PDF Upload & Display: Drag-and-drop or click-to-upload PDF files, rendered
  on canvas with responsive scaling.
- Text-to-Speech Reading: Sentence-by-sentence audio narration using the Web
  Speech API with voice selection and adjustable playback speed.
- Sentence-Level Navigation: Navigate through sentences independently of pages
  with Previous/Next Sentence controls.
- Bookmarking System: Save and resume reading from bookmarked positions with
  automatic progress tracking.
- Focus Mode: Minimize UI to reduce distractions during reading sessions.
- Interactive Text Selection: Tap/click to highlight and select individual
  sentences on the PDF.
- Auto-Resume: Automatically detect and offer to resume from the last reading
  position.
- Bookmark Export/Import: Export all bookmarks (across every PDF) as a JSON
  file download; import a saved file to merge bookmarks back without
  overwriting existing entries.
- View Mode Toggle: Switch between Fit Page (800px max-width) and Fit Width
  (full screen width) layouts; in Fit Width mode the view auto-scrolls to keep
  the currently reading sentence visible.

Tech Stack
----------
- Frontend: Vanilla HTML5/CSS3/JavaScript (no frameworks)
- PDF Rendering: PDF.js (v3.11.174) via CDN
- Speech Synthesis: Web Speech API (native browser API)
- Storage: Browser localStorage for bookmarks and reading progress
- Design: Dark theme with responsive UI

Architecture
------------
The entire application is contained in a single index.html file. It uses canvas
elements for PDF rendering and highlighting, with localStorage for persistence.
The interface includes a top control bar (collapsible in Focus mode), a main
content area with the PDF canvas, a sentence ticker for audio feedback, and
floating action buttons for exiting focus mode and controlling playback.

Changelog
---------
v1.1
- Moved the Save Bookmark button from the bottom controls to the top bar,
  next to the Bookmarks button.
- Added Export (⬇) and Import (⬆) buttons inside the bookmarks sheet to
  save/load all bookmarks across all PDFs as a JSON file. Import merges new
  entries without removing existing ones.
- Added a play/pause floating action button (FAB) in the bottom-right corner
  that is visible only in Focus mode, matching the state colour of the main
  play button (red = stopped, purple = paused, dark red = playing).

v1.2
- Added a Fit Width / Fit Page toggle button in the top bar. Fit Page
  (default) constrains the PDF to 800px wide; Fit Width removes that cap so
  the PDF fills the full screen width. In Fit Width mode the view
  automatically scrolls to keep the highlighted/reading sentence in view.
